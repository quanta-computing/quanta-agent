# Automatically added by dh_installinit
if [ -x "/etc/init.d/quanta-agent" ] || [ -e "/etc/init/quanta-agent.conf" ]; then
	if [ ! -e "/etc/init/quanta-agent.conf" ]; then
		update-rc.d quanta-agent defaults >/dev/null
	fi
	invoke-rc.d quanta-agent start || exit $?
fi
# End automatically added section
