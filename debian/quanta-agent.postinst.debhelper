# Automatically added by dh_installinit
if [ -x "/etc/init.d/quanta-agent" ]; then
	update-rc.d quanta-agent defaults >/dev/null
	invoke-rc.d quanta-agent start || exit $?
fi
# End automatically added section
