# Automatically added by dh_installinit
if [ -x "/etc/init.d/quanta-agent" ] || [ -e "/etc/init/quanta-agent.conf" ]; then
	invoke-rc.d quanta-agent stop || exit $?
fi
# End automatically added section
