name: "quanta-agent-process"
arch: "${ARCH}"
platform: "linux"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
version_schema: "none"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Centreon Experience Monitoring process plugin
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://www.centreon.com"
license: "Apache-2.0"

overrides:
  rpm:
    depends:
      - quanta-agent-base = ${VERSION}
  deb:
    depends:
      - quanta-agent-base (= ${VERSION})

contents:
  - src: /tmp/staging/@LIBDIR@/quanta/modules/process.so
    dst: /@LIBDIR@/quanta/modules/process.so

  - src: /tmp/staging/etc/quanta/modules.d/process.yml
    dst: /etc/quanta/modules.d/process.yml
    type: config|noreplace
