name: "quanta-agent-memory"
arch: "${ARCH}"
platform: "linux"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
version_schema: "none"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Centreon Experience Monitoring memory plugin
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://www.centreon.com"
license: "Apache-2.0"

overrides:
  rpm:
    depends:
      - quanta-agent-base = ${VERSION}
  deb:
    depends:
      - quanta-agent-base (= ${VERSION})

contents:
  - src: /tmp/staging/usr/lib/quanta/modules/memory.so
    dst: /usr/lib/quanta/modules/memory.so

  - src: /tmp/staging/etc/quanta/modules.d/memory.yml
    dst: /etc/quanta/modules.d/memory.yml
    type: config|noreplace
