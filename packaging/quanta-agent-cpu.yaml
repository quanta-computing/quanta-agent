name: "quanta-agent-cpu"
arch: "${ARCH}"
platform: "linux"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
version_schema: "none"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Centreon Experience Monitoring CPU plugin
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://www.centreon.com"
license: "Apache-2.0"

overrides:
  rpm:
    depends:
      - quanta-agent-base = ${VERSION}
  deb:
    depends:
      - quanta-agent-base (= ${VERSION})

contents:
  - src: /tmp/staging/@LIBDIR@/quanta/modules/cpu.so
    dst: /@LIBDIR@/quanta/modules/cpu.so

  - src: /tmp/staging/etc/quanta/modules.d/cpu.yml
    dst: /etc/quanta/modules.d/cpu.yml
    type: config|noreplace
