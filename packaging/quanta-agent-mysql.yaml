name: "quanta-agent-mysql"
arch: "${ARCH}"
platform: "linux"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
version_schema: "none"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Centreon Experience Monitoring MySQL plugin
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://www.centreon.com"
license: "Apache-2.0"

overrides:
  rpm:
    depends:
      - quanta-agent-base = ${VERSION}
      - mysql-libs
  deb:
    depends:
      - quanta-agent-base (= ${VERSION})
      - libmysqlclient21

contents:
  - src: /tmp/staging/@LIBDIR@/quanta/modules/mysqlstat.so
    dst: /@LIBDIR@/quanta/modules/mysqlstat.so

  - src: /tmp/staging/etc/quanta/modules.d/mysqlstat.yml
    dst: /etc/quanta/modules.d/mysqlstat.yml
    type: config|noreplace
